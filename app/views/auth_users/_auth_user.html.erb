<table class="form">
  	<tr>
	  <th><%= f.label :lab_id, 'Lab' %></th>
      <td><%= f.collection_select(:lab_id, @labs, :id, :lab_name, {:include_blank => 'Select..'}) %></td>
	</tr>
	
	<tr>
  	  <th><%= f.label :name %></th>
	  <td><%= f.text_field :name %></td>
	</tr>
	
	<tr>
	  <th><%= f.label :email %></th>
      <td><%= f.text_field :email %></td>
	</tr>
	
    <tr>
	  <th><%= f.label :notes %></th>
      <td><%= f.text_field :notes %></td>
	</tr>
</table>