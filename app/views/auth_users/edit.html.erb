<h1>Editing Authorized User</h1>

<% form_for(@auth_user) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'auth_user', :locals => {:f => f} %>
  
  <%= f.submit "Update" %>
<% end %>

<%= link_to 'Back', auth_users_path %>
